# Hearth 部署指南

## 前提条件

- 一台服务器（宝塔面板 / 纯 Linux 均可）
- Docker + Docker Compose 已安装
- 一个域名（可选，不设域名也可用 IP 访问）

---

## 一键部署

SSH 登录服务器，执行：

```bash
cd /opt
git clone https://github.com/motao123/Hearth.git
cd Hearth
bash scripts/deploy.sh
```

脚本会自动完成：
1. 生成随机 JWT 密钥
2. 写入 `.env` 配置
3. 构建并启动 Docker 容器
4. 输出 Nginx 反代配置（如需 HTTPS）

启动后访问 `http://服务器IP:8090` 即可使用。

---

## 配置 HTTPS（推荐）

如果你有域名并希望启用 HTTPS，在宝塔面板操作：

1. **添加站点** → 网站 → 添加站点 → 填入域名
2. **申请证书** → 站点设置 → SSL → Let's Encrypt → 申请
3. **粘贴配置** → 站点设置 → 配置文件 → 替换为 `deploy.sh` 输出的 Nginx 配置

完成后访问 `https://你的域名`。

> Docker 容器仅监听 `127.0.0.1:8090`，外部流量必须通过 Nginx 反代，确保安全。

---

## 手动部署（不用脚本）

如果你希望自己控制每一步：

```bash
cd /opt/Hearth

# 1. 生成密钥并写入 .env
cp .env.example .env
# 编辑 .env，至少设置 HEARTH_SECRET_KEY（32位以上随机字符串）
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# 把输出的字符串填入 .env 的 HEARTH_SECRET_KEY=

# 2. 启动
docker compose up -d --build

# 3. 验证
curl http://127.0.0.1:8090/api/health
```

---

## 环境变量说明

| 变量 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `HEARTH_SECRET_KEY` | **是** | — | JWT 密钥，>= 32 字符，不设置则无法启动 |
| `HEARTH_CORS_ORIGINS` | 否 | localhost | 允许的前端域名，逗号分隔 |
| `HEARTH_SESSION_EXPIRE_HOURS` | 否 | 72 | 登录会话有效期（小时） |
| `HEARTH_DEBUG` | 否 | false | 调试模式（开启后可访问 Swagger API 文档） |
| `HEARTH_UPLOAD_MAX_SIZE_MB` | 否 | 10 | 文件上传大小限制（MB） |
| `HEARTH_RATE_LIMIT_LOGIN` | 否 | 5/minute | 登录速率限制 |

---

## 常用命令

| 操作 | 命令 |
|------|------|
| 查看日志 | `docker compose logs -f` |
| 重启服务 | `docker compose restart` |
| 停止服务 | `docker compose down` |
| 更新代码 | `git pull && docker compose up -d --build` |
| 查看状态 | `docker compose ps` |
| 进入容器 | `docker compose exec hearth bash` |

---

## 数据备份

Docker 使用命名卷存储数据，更新/重启不会丢失。手动备份：

```bash
# 备份数据库
docker compose exec hearth cp /app/data/hearth.db /app/backups/hearth_$(date +%Y%m%d).db

# 从容器拷出
docker cp hearth:/app/backups/ ./hearth-backup/
```

自动备份（宝塔计划任务）：

```bash
#!/bin/bash
BK=/www/backup/hearth
mkdir -p $BK
docker cp hearth:/app/data/hearth.db "$BK/hearth_$(date +%Y%m%d).db"
find $BK -name "hearth_*.db" -mtime +7 -delete
```

---

## 故障排查

| 问题 | 排查 |
|------|------|
| 容器启动失败 | `docker compose logs` 查看错误日志 |
| 提示 SECRET_KEY 未设置 | 检查 `.env` 中 `HEARTH_SECRET_KEY` 是否填入 |
| 502 Bad Gateway | `curl http://127.0.0.1:8090/api/health` 确认后端在跑 |
| 无法上传文件 | 检查 Nginx 的 `client_max_body_size` 是否 >= 10m |
| 登录后跳回登录页 | 检查 `HEARTH_CORS_ORIGINS` 是否包含你的域名 |
