# Hearth 宝塔面板部署指南

## 环境要求

- 宝塔面板 7.x+
- Python 3.11+（宝塔软件商店安装）
- Node.js 18+（宝塔软件商店安装 PM2 管理器）
- Nginx（宝塔默认已装）

---

## 一、上传代码

```bash
# SSH 登录服务器
cd /www/wwwroot
git clone https://github.com/motao123/Hearth.git
cd Hearth
```

---

## 二、配置环境变量

```bash
cp .env.example .env
```

编辑 `.env`：

```ini
HEARTH_SECRET_KEY=你的随机密钥
HEARTH_CORS_ORIGINS=https://你的域名
HEARTH_DB_PATH=/www/wwwroot/Hearth/data/hearth.db
HEARTH_UPLOAD_DIR=/www/wwwroot/Hearth/data/uploads
```

生成随机密钥：

```bash
openssl rand -hex 32
```

---

## 三、构建前端

```bash
cd /www/wwwroot/Hearth/frontend
npm install
npm run build
# 产物在 frontend/dist/
```

---

## 四、配置后端

```bash
cd /www/wwwroot/Hearth/backend

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -e .

# 创建数据目录
mkdir -p /www/wwwroot/Hearth/data
mkdir -p /www/wwwroot/Hearth/backups
```

### 创建 systemd 服务

宝塔 → 文件 → `/etc/systemd/system/hearth.service`：

```ini
[Unit]
Description=Hearth Family Planner
After=network.target

[Service]
User=www
Group=www
WorkingDirectory=/www/wwwroot/Hearth/backend
EnvironmentFile=/www/wwwroot/Hearth/.env
ExecStart=/www/wwwroot/Hearth/backend/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8090 --workers 2
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

启动服务：

```bash
systemctl daemon-reload
systemctl enable hearth
systemctl start hearth

# 验证
systemctl status hearth
curl http://127.0.0.1:8090/api/health
```

---

## 五、配置 Nginx

宝塔 → 网站 → 添加站点：
- 域名：`hearth.你的域名.com`
- 根目录：`/www/wwwroot/Hearth/frontend/dist`

站点设置 → 配置文件，替换为：

```nginx
server {
    listen 80;
    server_name 你的域名;

    # 前端静态文件
    root /www/wwwroot/Hearth/frontend/dist;
    index index.html;

    # API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:8090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }

    # SPA 路由回退
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源长期缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 禁止访问隐藏文件
    location ~ /\. {
        deny all;
    }

    access_log /www/wwwlogs/hearth.log;
    error_log /www/wwwlogs/hearth.error.log;
}
```

---

## 六、配置 SSL

站点设置 → SSL → Let's Encrypt → 申请证书 → 勾选「强制 HTTPS」

---

## 七、设置权限

```bash
chown -R www:www /www/wwwroot/Hearth
chmod -R 755 /www/wwwroot/Hearth
chmod 700 /www/wwwroot/Hearth/data
```

---

## 八、定时备份

宝塔 → 计划任务 → 添加 Shell 脚本：

```bash
#!/bin/bash
BACKUP_DIR="/www/backup/hearth"
mkdir -p $BACKUP_DIR
cp /www/wwwroot/Hearth/data/hearth.db "$BACKUP_DIR/hearth_$(date +%Y%m%d).db"
find $BACKUP_DIR -name "hearth_*.db" -mtime +7 -delete
```

设置每天凌晨 3 点执行。

---

## 日常维护命令

| 操作 | 命令 |
|------|------|
| 查看日志 | `journalctl -u hearth -f` |
| 重启后端 | `systemctl restart hearth` |
| 更新代码 | `cd /www/wwwroot/Hearth && git pull && cd frontend && npm install && npm run build && cd ../backend && source venv/bin/activate && pip install -e . && systemctl restart hearth` |

---

## 网络不通排查

如果 Nginx 代理 API 报 502：

```bash
# 检查后端是否在跑
systemctl status hearth

# 测试后端端口
curl http://127.0.0.1:8090/api/health

# 检查防火墙
firewall-cmd --list-ports
```
