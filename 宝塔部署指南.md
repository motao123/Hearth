# Hearth 部署指南

## 方式一：一键启动（推荐）

无需 git clone，一条命令拉取镜像并部署：

```bash
bash <(curl -s https://raw.githubusercontent.com/motao123/Hearth/main/scripts/quick-start.sh)
```

脚本自动完成：拉取镜像 → 生成密钥 → 写入配置 → 启动容器 → 输出 Nginx 配置

---

## 方式二：手动 Docker 部署

```bash
# 1. 创建目录
mkdir -p /opt/hearth && cd /opt/hearth

# 2. 下载 docker-compose.yml
curl -O https://raw.githubusercontent.com/motao123/Hearth/main/docker-compose.quick.yml
mv docker-compose.quick.yml docker-compose.yml

# 3. 生成密钥并写入 .env
cat > .env << EOF
HEARTH_SECRET_KEY=$(python3 -c "import secrets; print(secrets.token_urlsafe(32))" || openssl rand -hex 32)
HEARTH_DEBUG=false
HEARTH_CORS_ORIGINS=https://你的域名
EOF

# 4. 启动
docker compose up -d

# 5. 验证
curl http://127.0.0.1:8090/api/health
```

---

## 方式三：从源码构建

```bash
cd /opt
git clone https://github.com/motao123/Hearth.git
cd Hearth
bash scripts/deploy.sh
```

适合需要自定义代码的用户。每次 `git pull` 后需重新 `docker compose up -d --build`。

---

## 配置 HTTPS（推荐）

有域名并希望启用 HTTPS 时，在宝塔面板操作：

1. **添加站点** → 网站 → 添加站点 → 填入域名
2. **申请证书** → 站点设置 → SSL → Let's Encrypt → 申请
3. **粘贴配置** → 站点设置 → 配置文件 → 替换为部署脚本输出的 Nginx 配置

完成后访问 `https://你的域名`。

> Docker 容器仅监听 `127.0.0.1:8090`，外部流量必须通过 Nginx 反代。

---

## 环境变量说明

| 变量 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `HEARTH_SECRET_KEY` | **是** | — | JWT 密钥，>= 32 字符，不设置则无法启动 |
| `HEARTH_CORS_ORIGINS` | 否 | localhost | 允许的前端域名，逗号分隔 |
| `HEARTH_SESSION_EXPIRE_HOURS` | 否 | 72 | 登录会话有效期（小时） |
| `HEARTH_DEBUG` | 否 | false | 调试模式（开启后可访问 Swagger API 文档） |
| `HEARTH_UPLOAD_MAX_SIZE_MB` | 否 | 10 | 文件上传大小限制（MB） |
| `HEARTH_RATE_LIMIT_LOGIN` | 否 | 5/minute | 登录速率限制 |

---

## 常用命令

| 操作 | 命令 |
|------|------|
| 查看日志 | `docker compose logs -f` |
| 重启服务 | `docker compose restart` |
| 停止服务 | `docker compose down` |
| 更新版本 | `docker compose pull && docker compose up -d` |
| 查看状态 | `docker compose ps` |
| 进入容器 | `docker compose exec hearth bash` |

---

## 数据备份

Docker 使用命名卷存储数据，更新/重启不会丢失。

手动备份：
```bash
docker cp hearth:/app/data/hearth.db ./hearth_$(date +%Y%m%d).db
```

自动备份（宝塔计划任务）：
```bash
#!/bin/bash
BK=/www/backup/hearth
mkdir -p $BK
docker cp hearth:/app/data/hearth.db "$BK/hearth_$(date +%Y%m%d).db"
find $BK -name "hearth_*.db" -mtime +7 -delete
```

---

## 故障排查

| 问题 | 排查 |
|------|------|
| 容器启动失败 | `docker compose logs` 查看错误日志 |
| 提示 SECRET_KEY 未设置 | 检查 `.env` 中 `HEARTH_SECRET_KEY` 是否填入 |
| 502 Bad Gateway | `curl http://127.0.0.1:8090/api/health` 确认后端在跑 |
| 无法上传文件 | 检查 Nginx 的 `client_max_body_size` 是否 >= 10m |
| 登录后跳回登录页 | 检查 `HEARTH_CORS_ORIGINS` 是否包含你的域名 |
| 拉取镜像慢 | 配置 Docker 镜像加速器或使用代理 |
