# Hearth 宝塔面板部署指南

## 环境要求

- 宝塔面板 7.x+
- Python 3.11+（宝塔软件商店安装）
- Node.js 18+（宝塔软件商店安装 PM2 管理器）
- Nginx（宝塔默认已装）

---

## 一、上传代码

```bash
# SSH 登录服务器
cd /www/wwwroot
git clone https://github.com/motao123/Hearth.git
cd Hearth
```

---

## 二、配置环境变量

```bash
cp .env.example .env
```

编辑 `.env`：

```ini
HEARTH_SECRET_KEY=你的随机密钥（至少32位）
HEARTH_CORS_ORIGINS=https://你的域名
HEARTH_DB_PATH=/www/wwwroot/Hearth/data/hearth.db
HEARTH_UPLOAD_DIR=/www/wwwroot/Hearth/data/uploads
HEARTH_DEBUG=false
```

生成随机密钥：

```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

> **安全提醒**：`HEARTH_SECRET_KEY` 为必填项，未设置或长度不足 32 位将导致服务无法启动。切勿使用 `.env.example` 中的默认值。

---

## 三、构建前端

```bash
cd /www/wwwroot/Hearth/frontend
npm install
npm run build
# 产物在 frontend/dist/
```

---

## 四、配置后端

```bash
cd /www/wwwroot/Hearth/backend

# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -e .

# 创建数据目录
mkdir -p /www/wwwroot/Hearth/data
mkdir -p /www/wwwroot/Hearth/backups
```

### 创建专用系统用户

```bash
# 创建专用用户（非 www，避免同服务器其他站点读取 Hearth 文件）
useradd -r -s /sbin/nologin hearth
```

### 创建 systemd 服务

宝塔 → 文件 → `/etc/systemd/system/hearth.service`：

```ini
[Unit]
Description=Hearth Family Planner
After=network.target

[Service]
User=hearth
Group=hearth
WorkingDirectory=/www/wwwroot/Hearth/backend
EnvironmentFile=/www/wwwroot/Hearth/.env
ExecStart=/www/wwwroot/Hearth/backend/venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8090
Restart=always
RestartSec=5

# 安全加固
NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/www/wwwroot/Hearth/data /www/wwwroot/Hearth/backups

[Install]
WantedBy=multi-user.target
```

> **注意**：`--host` 改为 `127.0.0.1`，仅监听本地回环，通过 Nginx 反向代理对外提供服务。

启动服务：

```bash
systemctl daemon-reload
systemctl enable hearth
systemctl start hearth

# 验证
systemctl status hearth
curl http://127.0.0.1:8090/api/health
```

---

## 五、配置 Nginx（强制 HTTPS）

宝塔 → 网站 → 添加站点：
- 域名：`hearth.你的域名.com`
- 根目录：`/www/wwwroot/Hearth/frontend/dist`

### 5.1 先申请 SSL 证书

站点设置 → SSL → Let's Encrypt → 申请证书

### 5.2 配置文件

站点设置 → 配置文件，替换为：

```nginx
# HTTP 强制跳转 HTTPS
server {
    listen 80;
    server_name 你的域名;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name 你的域名;

    # SSL 证书（宝塔自动填充路径）
    ssl_certificate    /www/server/panel/vhost/cert/你的域名/fullchain.pem;
    ssl_certificate_key /www/server/panel/vhost/cert/你的域名/privkey.pem;

    # SSL 加固
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;

    # 安全响应头
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self'; frame-ancestors 'none'" always;

    # 前端静态文件
    root /www/wwwroot/Hearth/frontend/dist;
    index index.html;

    # API 反向代理
    location /api/ {
        proxy_pass http://127.0.0.1:8090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;

        # 限制上传大小（与后端 upload_max_size_mb 对应）
        client_max_body_size 10m;
    }

    # SPA 路由回退
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源长期缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|woff2?)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 禁止访问敏感文件
    location ~ /\. {
        deny all;
    }
    location ~* \.(env|py|md|toml|ini|lock)$ {
        deny all;
    }

    access_log /www/wwwlogs/hearth.log;
    error_log /www/wwwlogs/hearth.error.log;
}
```

> **重要**：HTTP 80 端口仅做 301 跳转，所有流量必须走 HTTPS。SSL 证书路径由宝塔自动管理，请勿手动修改。

---

## 六、设置文件权限

```bash
# 整体属主设为 hearth 用户
chown -R hearth:hearth /www/wwwroot/Hearth

# 数据目录仅属主可读写执行
chmod 700 /www/wwwroot/Hearth/data
chmod 700 /www/wwwroot/Hearth/backups

# .env 文件仅属主可读写（防止其他用户读取密钥）
chmod 600 /www/wwwroot/Hearth/.env

# 前端静态文件允许 Nginx（www用户）读取
chmod -R 755 /www/wwwroot/Hearth/frontend/dist
```

> **安全说明**：
> - `.env` 文件包含 JWT 密钥，必须设为 `600`，禁止其他用户读取
> - 数据目录设为 `700`，仅 hearth 用户可访问
> - 不再使用 `chmod -R 755` 对整个项目授权，避免 `.env` 等敏感文件泄露

---

## 七、定时备份

宝塔 → 计划任务 → 添加 Shell 脚本：

```bash
#!/bin/bash
BACKUP_DIR="/www/backup/hearth"
mkdir -p $BACKUP_DIR

# 备份数据库
cp /www/wwwroot/Hearth/data/hearth.db "$BACKUP_DIR/hearth_$(date +%Y%m%d).db"

# 备份上传文件（如有）
if [ -d "/www/wwwroot/Hearth/data/uploads" ]; then
    tar czf "$BACKUP_DIR/uploads_$(date +%Y%m%d).tar.gz" -C /www/wwwroot/Hearth/data uploads
fi

# 清理 7 天前的备份
find $BACKUP_DIR -name "hearth_*.db" -mtime +7 -delete
find $BACKUP_DIR -name "uploads_*.tar.gz" -mtime +7 -delete
```

设置每天凌晨 3 点执行。

---

## 八、安全清单

部署完成后，请逐项确认：

| 检查项 | 状态 |
|--------|------|
| `HEARTH_SECRET_KEY` 已设置且 >= 32 字符 | ☐ |
| `.env` 文件权限为 `600` | ☐ |
| 数据目录权限为 `700` | ☐ |
| 服务以 `hearth` 专用用户运行（非 www/root） | ☐ |
| HTTP 强制跳转 HTTPS | ☐ |
| HSTS 头已启用 | ☐ |
| CSP 安全头已启用 | ☐ |
| uvicorn 仅监听 `127.0.0.1` | ☐ |
| Swagger API 文档已关闭（`HEARTH_DEBUG=false`） | ☐ |
| Nginx 禁止访问 `.env`/`.py` 等敏感文件 | ☐ |

---

## 日常维护命令

| 操作 | 命令 |
|------|------|
| 查看日志 | `journalctl -u hearth -f` |
| 重启后端 | `systemctl restart hearth` |
| 更新代码 | `cd /www/wwwroot/Hearth && git pull && cd frontend && npm install && npm run build && cd ../backend && source venv/bin/activate && pip install -e . && systemctl restart hearth` |
| 检查服务状态 | `systemctl status hearth` |
| 验证 HTTPS | `curl -I https://你的域名/api/health` |

---

## 网络不通排查

如果 Nginx 代理 API 报 502：

```bash
# 检查后端是否在跑
systemctl status hearth

# 测试后端端口
curl http://127.0.0.1:8090/api/health

# 检查防火墙
firewall-cmd --list-ports

# 检查 hearth 用户是否有数据目录写权限
ls -la /www/wwwroot/Hearth/data/

# 检查 .env 是否可读
sudo -u hearth cat /www/wwwroot/Hearth/.env
```
